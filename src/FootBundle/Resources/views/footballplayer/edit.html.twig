{% extends 'base.html.twig' %}

{% block body %}
    <h1>FootballPlayer edit</h1>

    <div class="row">
        <div class="col s6 offset-s3">
            {{ form_start(edit_form, { 'attr': {'class': 'form_create col s12'} }) }}

            <div class="input-field col s12">
                <div class="row">
                    {{ form_label(edit_form.name) }}
                    {{ form_errors(edit_form.name) }}
                    {{ form_widget(edit_form.name) }}
                </div>
            </div>
            <div class="input-field col s12">
                <div class="row">
                    {{ form_label(edit_form.prenom) }}
                    {{ form_errors(edit_form.prenom) }}
                    {{ form_widget(edit_form.prenom) }}
                </div>
            </div>
            <div class="input-field col s12">
                <div class="row">
                    {{ form_label(edit_form.size) }}
                    {{ form_errors(edit_form.size) }}
                    {{ form_widget(edit_form.size) }}
                </div>
            </div>
            <div class="input-field col s12">
                <div class="row">
                    {{ form_label(edit_form.age) }}
                    {{ form_errors(edit_form.age) }}
                    {{ form_widget(edit_form.age) }}
                </div>
            </div>
            <div class="input-field col s12">
                {{ form_errors(edit_form.post) }}
                {{ form_widget(edit_form.post) }}
                {{ form_label(edit_form.post) }}
            </div>
            <div class="col s12 file-field input-field">
                <div class="btn">
                    <span>PHOTO</span>
                    {{ form_widget(edit_form.file) }}
                </div>
                <div class="file-path-wrapper">
                    <input class="file-path validate" type="text" placeholder="Ajouter une photo">
                </div>
            </div>
            <div class="form_bouton">
                <button class="btn waves-effect waves-light" type="submit" name="action">Modifier le joueur
                    <i class="material-icons right"></i>
                </button>
                <a class="waves-effect waves-light btn" href="{{ path('footballplayer_index') }}">Retour à la liste des joueurs</a>
            </div>

            {{ form_end(edit_form) }}

            <div class="form_bouton">
                {{ form_start(delete_form) }}
                <button class="btn waves-effect waves-light" type="submit" name="action">Supprimer le joueur</button>
                {{ form_end(delete_form) }}
            </div>
        </div>

    </div>
{% endblock %}