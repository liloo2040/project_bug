{% extends 'base.html.twig' %}

{% block body %}
    <h1>Team edit</h1>

    <div class="row">
        <div class="col s6 offset-s3">
            {{ form_start(edit_form, { 'attr': {'class': 'form_create col s12'} }) }}

            <div class="input-field col s12">
                <div class="row">
                    {{ form_label(edit_form.name) }}
                    {{ form_errors(edit_form.name) }}
                    {{ form_widget(edit_form.name) }}
                </div>
            </div>
            <div class="input-field col s12">
                <div class="row">
                    {{ form_label(edit_form.city) }}
                    {{ form_errors(edit_form.city) }}
                    {{ form_widget(edit_form.city) }}
                </div>
            </div>
            <div class="input-field col s12">
                {{ form_errors(edit_form.footballplayer) }}
                {{ form_widget(edit_form.footballplayer) }}
                {{ form_label(edit_form.footballplayer) }}
            </div>
            <div class="col s12 file-field input-field">
                <div class="btn">
                    <span>PHOTO</span>
                    {{ form_widget(edit_form.file) }}
                </div>
                <div class="file-path-wrapper">
                    <input class="file-path validate" type="text" placeholder="Ajouter une photo">
                </div>
            </div>
            <div class="form_bouton">
                <button class="btn waves-effect waves-light" type="submit" name="action">Modifier l'équipe
                    <i class="material-icons right"></i>
                </button>
                <a class="waves-effect waves-light btn" href="{{ path('team_index') }}">Retour à la liste des équipes</a>
            </div>

            {{ form_end(edit_form) }}

            <div class="form_bouton">
                {{ form_start(delete_form) }}
                <button class="btn waves-effect waves-light" type="submit" name="action">Supprimer l'équipe</button>
                {{ form_end(delete_form) }}
            </div>
        </div>
    </div>
{% endblock %}
